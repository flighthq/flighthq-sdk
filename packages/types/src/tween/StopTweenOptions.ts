export interface StopTweenOptions {
  /** Apply final property values to the target before stopping. Default: false. */
  complete?: boolean;
  /** Fire onComplete when complete is true. Default: true. */
  sendEvent?: boolean;
}
