{
  "name": "@flighthq/types",
  "version": "1.0.0",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean",
    "test": "vitest --config vitest.config.ts",
    "test:watch": "vitest --watch --config vitest.config.ts",
    "test:run": "vitest run --config vitest.config.ts"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  },
  "description": "Shared TypeScript interfaces, enums, and symbol constants used across all packages"
}
